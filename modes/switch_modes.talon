# TODO: re-define this later
# mode: dictation
# mode: command
# -
# ^dictate mode$:
#     mode.disable("sleep")
#     mode.disable("command")
#     mode.enable("dictation")
#     user.code_clear_language_mode()
#     mode.disable("user.gdb")
# ^command mode$:
#     mode.disable("sleep")
#     mode.disable("dictation")
#     mode.enable("command")
